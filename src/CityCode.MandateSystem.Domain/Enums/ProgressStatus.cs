namespace CityCode.MandateSystem.Domain.Enums
{
    public enum ProgressStatus
    {
        SENT_TO_NIBBS,
        NIBBS_APPROVED,
        NIBBS_REJECTED,
        NIBBS_ERROR,
        SENT_TO_TRANSACTION_QUEUE,
    }
}