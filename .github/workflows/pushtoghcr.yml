<<<<<<< HEAD
<<<<<<< HEAD
name: Build and Push Docker Image to GHCR
=======
name: Deploy to EC2 on Staging
>>>>>>> a352479 (Create pushtoghcr.yml)
=======
name: Build and Push Docker Image to GHCR
>>>>>>> bcced53 (Update pushtoghcr.yml)

on:
  push:
    branches:
      - staging

jobs:
  push_to_ghcr:
    runs-on: ubuntu-latest
<<<<<<< HEAD
<<<<<<< HEAD
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set owner to lowercase
        id: string
        run: echo "owner_lc=$(echo ${{ github.repository_owner }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT
=======

=======
>>>>>>> bcced53 (Update pushtoghcr.yml)
    permissions:
      contents: write  # Needed to commit the version bump
      packages: write  # Needed to push to GHCR
    steps:
<<<<<<< HEAD
      - name: Checkout code
        uses: actions/checkout@v3
>>>>>>> a352479 (Create pushtoghcr.yml)

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
=======
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Fetch all history for accurate version bumping
          fetch-depth: 0
>>>>>>> bcced53 (Update pushtoghcr.yml)

      - name: Read & Increment Version
        id: version
        run: |
          # Create the VERSION file if it doesn't exist
          if [ ! -f VERSION ]; then
            echo "000000" > VERSION
          fi
          
          CURRENT_VERSION=$(cat VERSION)
          # Increment the version number safely
          NEW_VERSION=$(printf "%06d" $((10#$CURRENT_VERSION + 1)))
          
          echo "Old version: $CURRENT_VERSION, New version: $NEW_VERSION"
          echo $NEW_VERSION > VERSION
          echo "VERSION=$NEW_VERSION" >> $GITHUB_ENV
      - name: Commit Updated Version
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add VERSION
          # Check if there are changes to commit
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Bump version to ${{ env.VERSION }}"
            git push
          fi
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }} # Use the built-in, secure token

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./NumberClassificationApi/Dockerfile
          push: true
<<<<<<< HEAD
<<<<<<< HEAD
          # --- MODIFIED LINE ---
          # Tags the image with the unique run number (e.g., 1-dev, 2-dev, etc.)
          tags: |
                ghcr.io/${{ steps.string.outputs.owner_lc }}/mandateapi:${{ github.run_number }}-dev
                ghcr.io/${{ steps.string.outputs.owner_lc }}/mandateapi:dev-latest
=======
          tags: ghcr.io/kiki-user1/mandateapi:${{ github.sha }}

      - name: Deploy to EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            docker login ghcr.io -u kiki-user1 -p ${{ secrets.GITHUB_TOKEN }}
            docker pull ghcr.io/kiki-user1/mandateapi:${{ github.sha }}
            docker stop HngStageOneProject || true
            docker rm HngStageOneProject || true
            docker run -d --name mandateapi -p 80:80 ghcr.io/kiki-user1/mandateapi:${{ github.sha }}
>>>>>>> a352479 (Create pushtoghcr.yml)
=======
          tags: ghcr.io/${{ github.actor }}/hngstage:${{ env.VERSION }}
>>>>>>> bcced53 (Update pushtoghcr.yml)
